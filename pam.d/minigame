# This is the example of PAM configuration

# Put pam module into /lib/security or set custom path
# Put your configuration into /etc/pam.d
# Set path to minigame
auth            required        pam-minigame.so PATH_TO_GAME_EXECUTABLE

# Pattern for lazy testers
# auth    required    PATH_TO_REPOSITORY/build/pam-minigame.so PATH_TO_REPOSITORY/build/game/asteroids
